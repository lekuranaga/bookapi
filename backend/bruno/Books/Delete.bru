meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/{{apiVersion}}/books/{{lastBookId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 204", function () {
    expect(res.status).to.equal(204);
  });
}
